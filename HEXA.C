#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<math.h>
void main()
{
	char a[20];
	printf("\n enter the number");
	gets(a);
	int l = strlen(a);
	for(int i=0;a[i]!='\0';i++)
	{
		k
	}
	getch();
}